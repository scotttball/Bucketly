<div class="row">
  <div class="col s10 offset-s1">
    <% if @posts.any? %>
      <% @posts.each do |post| %>
        <p><%= post.title %></p>
        <p><%= post.description %></p>
      <% end %>
    <% else %>
      <h1>No Posts</h1>
    <% end %>
  </div>
</div>
